import React from 'react';

const Launch = () => {
  return (
    <section className='w-full h-screen flex justify-center items-center'>
        <span className="text-6xl font-kanit">Launch</span>
    </section>
  );
}

export default Launch;
