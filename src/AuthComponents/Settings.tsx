import React from 'react';

const Settings = () => {
  return (
    <section className='w-full h-screen flex justify-center items-center'>
      <span className='text-6xl font-kanit'>Settings</span>
    </section>
  );
}

export default Settings;
